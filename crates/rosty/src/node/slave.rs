use super::NodeArgs;

/// Slave API for a ROS node
pub struct Slave {
    name: String,
    uri: String,
}

impl Slave {
    /// Constructs a new slave for a node with the given arguments.
    pub fn new(args: &NodeArgs) -> Slave {

    }
}